---
name: ship
description: Test, build, deploy Site Diary to production, and verify it live. Use for any "deploy", "ship", "push to prod" request, or after completing a feature.
---

# Ship Site Diary to production

Follow `docs/ship.md` in the repo root. It is the single source of truth for this
procedure and is shared with every agent that works on this codebase — read it and
execute it step by step, stopping on any failure.

Do not reproduce the steps here. If the procedure changes, change `docs/ship.md`.
